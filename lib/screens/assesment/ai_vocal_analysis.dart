import 'package:flutter/material.dart';
class AiVocalAnalysisScreen extends StatelessWidget {
  const AiVocalAnalysisScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("AI Vocal Analysis Screen")),
    );
  }
}